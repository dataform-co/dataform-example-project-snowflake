config {
  type: "table",
  schema: "sources",
  tags: ["staging", "daily"],
  description: "Cleaned version of the raw crm.customer table."
}

select
  id,
  first_name,
  last_name,
  email,
  country
from
  ${ref("crm", "customer")}

  